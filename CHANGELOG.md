# Changelog

## Unreleased

### Fixed

- 玩家存档 ID 仅接受数字 QQ，阻止路径穿越输入。
- 同一玩家的修炼、突破、购药、服药和修习功法改为串行事务，避免并发覆盖存档。
- 存档继续使用原子替换写入；突破与物品 buff 统一为单次落盘。
- 修复凡人境界 `nextExp=0` 导致无法正常积累修为的问题，并兼容迁移旧存档。
- 配置中的显式 `0` 现在会被正确保留；灵石数量区间、突破损失比例和购买数量会校验。
- 大境界突破失败使用已有 `majorFailLoseRatio` 配置，不再写死跌层逻辑。
- 安装脚本在依赖安装失败时正确退出，并保留 Git 元数据以支持自动更新。

### Changed

- 玩家存档由独立 JSON 文件改为 `data/xiuxian.sqlite3`，使用 WAL、事务和索引持久化。
- 玩家读取接入 Yunzai 全局 Redis，缓存键为 `xiuxian:player:<玩家ID>`，默认 TTL 为 30 秒；写入和删除后同步更新缓存。
- Redis 未连接或临时故障时自动回源 SQLite，Redis 不作为数据源，不影响存档安全。
- 排行榜直接查询 SQLite 排名索引，不再扫描全部 JSON 文件或依赖进程内列表缓存。
- 增加玩家 schema 迁移层、集中配置 schema 与命令错误边界。
- 插件根目录改由模块位置定位，不再依赖进程当前工作目录。
- 锅巴配置整批校验、单次原子保存。
- 运行环境调整为 Node.js 22.13.0 或更高版本，以使用无需启动参数的内置 `node:sqlite`。

### Compatibility

- 首次启动自动事务导入 `data/players/*.json`，支持 QQ、OpenID 和 `QQ:OpenID` 文件名。
- 导入成功后，旧目录会改名为 `players.json-backup-时间戳`，不会自动删除；损坏的 JSON 会使整批迁移回滚。
- 已存在的 SQLite 玩家记录不会被旧 JSON 覆盖，避免旧备份污染新数据。
- 新存档保留 `schemaVersion: 1` 和完整 JSON payload，未知扩展字段不会丢失。
- 默认玩法数值不变。凡人的零经验瓶颈按下一境界基础值 100 修复，这是对不可游玩状态的纠正。
